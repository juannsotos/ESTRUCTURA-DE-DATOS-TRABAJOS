arreglo = [40,50,60,70,80,90]
print("Recorrido Inverso del array: ", end="")
print(" \nLos elementos del array son: ",end="")
for i in range(len(arreglo)-1,-1,-1): # el primero es pa quitarle 1 al range y el otro es pa saber donde se va detener el ciclo y el ultimo es el paso con el que lo va a hacer
    print (arreglo[i], "", end="")
print()
# tarea estructura de datos
#hacer un algoritmo para hacer el recorrido de un arreglo e imprimir sus valores

#otro algoritmo donde se inserta un valor en un indice determinado, digamos

#un algoritmo para la busqueda de un valor, pero la busqueda,
